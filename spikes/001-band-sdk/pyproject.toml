[project]
name = "001-band-sdk"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "band-sdk[pydantic-ai]>=1.0.0",
    "python-dotenv>=1.2.2",
]
