# Cover Image — A2A Knowledge Mesh

## Prompt (DALL-E 3 / Midjourney / Stable Diffusion)

```
A dark tech banner with a split-screen layout. Left side: a terminal window showing
"mesh graphify" output — a table of 6 conflicting version numbers across different
sources (README: Python 3.9, pyproject.toml: >=3.11, CI: 3.12, Dockerfile: 3.10),
each row highlighted with a red conflict badge. Right side: a frosted-glass sidebar
panel with glassmorphism effect showing 3 stats (Subjects: 1, Facts: 8, Conflicts: 3)
and a list of red conflict items labeled "python-version", "package-manager",
"auth-provider". Dark navy background (#0b0f19), blue accent (#3b82f6), green
(#10b981), red (#ef4444). Clean, minimal, looks like a real developer tool UI.
Inter font. No generic network graph, no sci-fi elements. 16:9. No text except
the terminal output and stats.
```

## Alternative — Graph focus

```
Dark developer tool interface. Large interactive network graph dominated by one
large blue circular node labeled "runtime" in the center, connected by labeled
edges (python-version, package-manager) to smaller green and red nodes. Green
nodes are valid facts; red nodes have a subtle glow effect and dashed connection
lines. Sidebar shows conflict panel with 3 red items. Labels use monospace font.
Background: radial gradient dark blue to black (#0b0f19). Blue (#3b82f6), green
(#10b981), red (#ef4444). Looks like a real Vis.js network graph visualization.
16:9. Professional dev tool aesthetic.
```

## Aspect Ratio

16:9 (1920×1080 px)

## Generation Notes

- Use DALL-E 3 or Midjourney v6
- The key is making it look like a real product screenshot, not concept art
- No floating 3D agents, no ethereal robots, no glowing orbs in space
- Should look like it could be the hero image on a GitHub README
